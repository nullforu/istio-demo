# Backend Service (공통)
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: istio-demo
  labels:
    app: backend
spec:
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  selector:
    app: backend
